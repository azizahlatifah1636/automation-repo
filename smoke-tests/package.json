{
  "name": "smoke-tests",
  "version": "1.0.0",
  "description": "Smoke tests for deployment verification",
  "scripts": {
    "test:staging": "jest --config=jest.staging.config.js",
    "test:production": "jest --config=jest.production.config.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "jest": "^29.6.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.22.9",
    "babel-jest": "^29.6.1"
  }
}
